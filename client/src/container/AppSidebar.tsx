"use client";

/**
 * AppSidebar — Legacy container kept for reference.
 * The main sidebar is now fully inline in src/app/page.tsx using shadcn Sidebar.
 * This file is no longer used but kept to avoid import resolution errors.
 */

export default function AppSidebar() {
  return null;
}
